[project]
name = "aoscc"
version = "2026.6.28.0"
dependencies = [
    "flask",
    "python-telegram-bot",
    "requests",
    "pillow",
    "PyNaCl",
    "regex",
    "qrcode",
    "gunicorn",
]
requires-python = ">=3.12"
authors = [
  {name = "TechCiel", email = "ciel@aosc.io"},
]

[tool.setuptools]
packages = ["aoscc"]

[project.scripts]
aoscc = "aoscc:main"
